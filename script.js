document.querySelectorAll('.dot').forEach(dot=>{const card=document.getElementById(dot.dataset.id);const act=()=>{document.querySelectorAll('.card').forEach(x=>x.classList.remove('active'));card&&card.classList.add('active')};dot.addEventListener('mouseenter',act);dot.addEventListener('focus',act);dot.addEventListener('click',act)});document.querySelectorAll('img').forEach(img=>img.addEventListener('error',()=>img.style.display='none'));